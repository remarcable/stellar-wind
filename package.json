{
  "name": "stellar-wind",
  "version": "0.0.1",
  "description": "Listen to the beauty of Stellar transactions",
  "main": "index.html",
  "scripts": {
    "predeploy": "parcel build index.html --public-url ./",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "license": "MIT",
  "dependencies": {
    "mo-js": "^0.288.2",
    "stellar-sdk": "^0.8.0",
    "tone": "^0.11.11"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "parcel-bundler": "^1.6.2"
  }
}
